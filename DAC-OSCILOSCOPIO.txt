#define DAC_PIN 25

void setup() {}

void loop() {
  for (int i = 0; i <= 255; i++) {
    dacWrite(DAC_PIN, i);
    delayMicroseconds(50);
  }

  for (int i = 255; i >= 0; i--) {
    dacWrite(DAC_PIN, i);
    delayMicroseconds(50);
  }
}